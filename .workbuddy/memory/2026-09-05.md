# 2026-09-05

## 天语言（Tian）项目启动
- 目录：/Users/samly/T/tian/
- 定稿《天语言规范 v0.1》：tian/天语言规范.md（.t 后缀、编译器 tc、/a 只读 //b 可变、w/ i/ f/ 流程标记、打印用反引号、分词斜杠规则见规范 2.7）
- 创建 tc 编译器 Rust 工程：tian/tc/（nom 7 依赖），模块骨架 main.rs / lexer.rs / ast.rs / parser.rs / type_check.rs / gen_c.rs
- lexer.rs 完成并通过 8 个单测：核心斜杠歧义逻辑（`//`+字母=可变声明、`/`+字母=只读声明、其余=除号）、w/i/f 保留标记字、注释、数字/字符串/浮点校验（1.5x、3./2 报错）
- cargo 在 ~/.cargo/bin/（shell PATH 中没有，用绝对路径）
- **遗留决策点**：规范 2.7 纯前看规则导致 main.t 中 `cnt/a` 被判为声明（/后是字母），表达式内"变量/变量"除法无法书写；需 Sam 决定：改样例写法、或引入"前一个 Token 是标识符/数字/右括号则为除号"的上下文规则（后者需修订规范 2.7）
- 下一步：开发顺序第 2 步——parser.rs（Token→AST）

## 天语言 v0.1 原型完成（同日晚）
- 斜杠规则按方案 2 修订为 v0.1.1（表达式内 / 恒为除号），规范 2.7/4/5.3/9 已同步更新
- 四大模块全部实现并通过 22 个单测：parser.rs（递归下降）、type_check.rs（ty_of 即推导即校验）、gen_c.rs（中文标识符 mangle 为 t_x5929 形式）、main.rs（lex/--emit-c/run 三命令）
- `tc run main.t` 端到端输出 10/0/0/0/100 与规范预期一致；错误行号在语句开始处捕获（曾偏移+1已修）
- v0.1 约束（已写入规范）：函数参数固定 i64、函数无返回值、函数体不可访问顶层变量、i32 标注仅接受范围内整数字面量
- release 二进制：tian/tc/target/release/tc；日常验证命令 `tc run xxx.t`
- 后续路线：LLVM 后端（tc build）、返回值、else、借用检查

## 天语言 v0.2（同日夜）
- 新增：r/ 返回（固定 i64，裸 r/ 返回 0，函数末尾隐式 r/0）、e/ else 分支、函数调用可作表达式（类型 i64）；保留标记字增至 w/i/e/f/r 五个且受表达式上下文门控（规范 2.2/5.2/5.3）
- tc build 落地：C 后端 + cc/gcc 链接生成原生可执行文件（LLVM 为后续阶段）；函数 C 签名改为 static long long
- 已知取舍（规范已载）：表达式内对名为 e/r 的变量做除法会被误判为标记，须写 (e)/y 规避
- 23 单测全绿；v02.t 端到端：返回值打印、max 用 else、嵌套调用算术 `add(1,2)*10+max(5,6)`=36 全对；main.t 回归通过
- 下一步候选：LLVM/Cranelift 原生后端（摆脱 cc 依赖）、参数/返回值类型标注、str 串联、字符串作为一等参数

## 天语言 v0.3（同日深夜）
- 全量类型标注：f/名(参:类型):返回类型，五种类型可用，省略默认 i64；隐式提升 I32→I64、整数→F64、I32 字面量范围特判（value_assignable 统一传参/返回/声明三处）
- str + str 拼接（gen_c 运行时 __t_cat malloc 实现，仅支持 +）；str 与数字混算报错并提示用 tos()
- 关键 bug：emit_expr 曾对调用参数硬编码 (long long) 强转，str/f64 参数全毁——已去除强转（type_check 保证兼容性，C 隐式转换即可）；教训：后端强转应跟随签名而非写死
- 26 单测全绿；v03.t 验证 str 函数/f64 阶乘循环/fact(10)=3628800/拼接/build 产物一致
- 语言核心（v1.0 范围）已齐：类型系统、str、控制流、函数、原生可执行文件。剩余大项：Cranelift/LLVM 原生后端（去 cc 依赖）、所有权/借用（天权 v2）、标准库

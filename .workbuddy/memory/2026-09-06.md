# 2026-09-06

## 天语言 v1.1 —— Cranelift 原生后端
- tc build 现直出宿主机器码（.o），不再经 C 中转；与独立运行时 tc/src/tian_rt.c 链接（cc 仅作链接器+启动器，与 rustc 策略一致），失败自动回退 C 后端
- 新增 gen_native.rs（~550 行）：FunctionBuilder/Variable 槽位变量、while/if/else 块结构（brif/jump/seal）、模块级字符串字面量池（DataDescription 只读数据段+去重）、运行时导入 __t_print_*/__t_tos_*/__t_cat/__t_seq/__t_sne
- 依赖：cranelift-codegen/frontend/module/object/native 0.116.1；API 要点：ObjectBuilder::new(isa,name,libcall_names)（无 symbol_scope）、declare_function→Result<FuncId>（非元组）、declare_data(…,tls:bool)、Variable::from_u32+declare_var(var,ty)、ins().jump(block,&[])、指针类型即 types::I64（无 R64）、builder.finish() 返回 Result、cranelift_module::default_libcall_names
- 双后端交叉验证抓到 3 个 bug 并修复：① gen_c str ==/!= 误用 C 指针比较（改 __t_seq/__t_sne）；② bool 打印不一致（统一 true/false，C 后端 fmt 改 %s + __t_tos_b）；③ 原生后端字符串池按函数计数导致 __t_str0 重名（改模块级池+去重）
- 解析器改进：块内最后一条语句可省略换行（} 可终止语句）
- 26 单测全绿；final.t 全特性双后端输出 diff 完全一致；main.t 双后端回归通过
- macOS 链接有无害警告 "no platform load command"（cranelift .o 缺 min version 记录，可忽略或后续补 LC_BUILD_VERSION）
- 下一步：天权 v2.0（所有权/借用）、int 除零陷阱语义确认、str 更完整标准库、数据结构（struct/数组）

## 天语言 v2.0 天权完成（下午）
- 修复 type_check copy() 语义 Bug：规范 11.2.4 规定 copy 参数只借用，原实现误调 mark_top_move；新增 TyLookup trait 使 ty_of 同时服务 VarInfo 表与纯 Ty 表
- type_check.rs 上移共享 collect_str_decls / consumes_var；新增 7 个所有权单元测试（use-after-move、复活、copy 借用、分支保守移动等），共 34 测试全绿
- gen_c.rs：修复 binds_dup or-pattern 编译错误；Decl 走"free 旧槽→绑定→置空源"、Assign 走临时变量块（RHS 可读自身旧值）、Return 走 tmp 块保证置空先于返回；str 隐式返回改 __t_dup("")（原 NULL 打印即 UB）；自我消费赋值 a=g(a) 跳过 free 防双重释放
- gen_native.rs 完整天权代码生成：入口 NULL 槽提升（循环内声明安全）、移动 def_var 置空、函数出口释放 str 声明+str 形参、__t_dup/__t_free 运行时导入
- 双后端交叉验证：main.t（10 0 0 0 100）与 own.t 验收程序 12 行输出完全一致；负向用例（use-after-move/只读复活/copy 类型错）均编译期拦截
- 关键教训：并行 Edit 同一文件会静默丢失部分修改，必须串行编辑

## 天语言 v2.1/v2.3/v2.2 全部完成
- v2.1 只读借用：`&s`/`&str` 形参；借用不移动不释放不可逃逸；`copy(&s)` 逃逸唯一路径
- v2.3 结构化差分诊断：`tc xxx.t --diagnose-json`（stage/error/line/message/hint/candidates，编辑距离候选）
- v2.2 语义锚点：`@pre:`/`@post:`/`@example:`（函数体头部）；@pre 入口守卫、@post 经 helper 在每个返回点校验（ret 伪变量，内部名 #ret）、@example 启动自检；`tc test` 只跑自检；tian_rt.c 新增 __t_check
- 修复 3 个真 bug：
  1. ty_of 与 check_expr 是两条并行的调用实参校验路径——check_expr 已接受 Str 字面量→&str 形参，但 ty_of 走 value_assignable 仍拒绝，导致 gen_c.rs 里 ty_of().unwrap() 在 Print(带字面量的调用) 处 panic。修复：value_assignable 增加 (BorrowStr, Str, Expr::Str) 特判。教训：新增类型规则必须同时改 check_expr 和 ty_of 两条路径
  2. gen_native @example 自检：__t_seq 已返回 I32 又对其 uextend(I32)——同宽扩展触发 Cranelift Verifier 错误。修复：Str 路径跳过 uextend
  3. 借用注释用 // 会与可变声明语法冲突，天语言行注释是 #
- 验收：/tmp/anchor.t 双后端输出一致（天! lit! 天!?）；负向测试 @pre 违规两后端均 exit(1) 带契约信息；main.t/own.t 双后端回归一致；cargo test 40 全绿
- 规范：新增 §12 语义锚点、§13 结构化差分诊断；修订记录补 v2.2/v2.3
- Cranelift 调试技巧：Verifier 错误信息不含细节时，按特性拆分源文件二分（只 @pre / 只 @post / 只 @example）快速定位
- @post helper 命名：`__t_post_` + cname（cname 自带 t_ 前缀）→ 实际 `__t_post_t_wrap`
